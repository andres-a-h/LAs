#include <stdio.h>
#include <stdlib.h>
#include "la1helpers.h"

// the main program that will be run by the user
int main() {
    int sol;
    // start with the instructions
    instructions(sol);


    return 0;
}
